# Security Log Analyzer & Intrusion Detection Tool

This project is a Python-based defensive security tool that analyzes authentication logs to detect suspicious activity such as brute-force login attempts.

## Features
- Authentication log parsing
- Brute-force detection
- IP-based attack correlation
- JSON alert reporting
- CLI-based execution

## Usage

```bash
python3 analyzer.py --log sample_logs/auth.log
